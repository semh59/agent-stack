# This workflow has been superseded by ci.yml (2026-04-19).
# The old multi-OS matrix referenced scripts and binaries that don't exist in
# this repo (script/build-schema.ts, scripts/build_binary.py) and would fail.
#
# Keeping the file as a stub so any protected-branch rules that reference it
# don't block merges. All real CI runs in .github/workflows/ci.yml.
name: Sovereign AI - Production Deployment Matrix (deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect notice
        run: |
          echo "This workflow is deprecated. See .github/workflows/ci.yml for the active pipeline."
